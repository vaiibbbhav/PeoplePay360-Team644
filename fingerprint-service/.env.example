# Database connection URL for PostgreSQL / Neon
DATABASE_URL=postgresql://user:password@host/neondb?sslmode=require

# 256-bit AES-GCM Master Key for Biometric Template Protection
# Generate a new 64-char hex key with: openssl rand -hex 32
FINGERPRINT_ENCRYPTION_KEY=your_256_bit_hex_key_here
